---
description: Run this workflow whenever a bug is found
---

Run `find_bugs_by_file` and `semantic_search` mcp tools to find similar bugs and fixes already reported on the graphs. If no bugs are found, save the current one and it's fix.